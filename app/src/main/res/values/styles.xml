<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowAnimationStyle">@style/app_animation</item>

    </style>

    <style name="app_animation" parent="@android:style/Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@anim/slide_in_right</item>
        <item name="android:activityOpenExitAnimation">@anim/slide_out_left</item>
        <item name="android:activityCloseEnterAnimation">@anim/slide_in_left</item>
        <item name="android:activityCloseExitAnimation">@anim/slide_out_right</item>
    </style>

    <style name="priceStyle" parent="TextAppearance.AppCompat.Body1">
        <item name="android:textColor">@color/gray_like</item>
        <item name="android:fontFamily">@font/proxima_nova_light</item>
    </style>

    <style name="priceOriginalStyle" parent="TextAppearance.AppCompat.Body1">
        <item name="android:textColor">@color/enjoei_red</item>
        <item name="android:fontFamily">@font/proxima_nova_light</item>
    </style>

    <style name="nameStyle" parent="TextAppearance.AppCompat.Body1">
        <item name="android:textColor">@color/gray_text</item>
        <item name="android:fontFamily">@font/proxima_nova_light</item>
    </style>

    <style name="likeStyle" parent="TextAppearance.AppCompat.Body1">
        <item name="android:textColor">@color/gray_like</item>
        <item name="android:fontFamily">@font/proxima_nova_light</item>
    </style>

</resources>
